/* ***************************************************************																** 	Main.c														**	Team 5														**	1/21/14 Tai Tiet: Initial Creation							**	1/22/14 Tai Tiet: Modified Main, added Menu, added Logfile 	**					  Advance functions noted 					**																** 	Main loop and Menu Options									**																**																** ************************************************************* */#include <stdio.h>#include <stdlib.h>#include "Struct.h"#include "Functions.h"#include "Constants.h"#include <string.h>/* print a menu */void Menu();int ReadLog (char fname[SLEN]);/* Test function */int main(){	int choose = 0;	/* variable to choose menu */	int bw = 0 ;	/* variable to choose black or white */	int y =	0;	/* variable for log file */	/* char fstart[4];*/ /* variable for position start */ 	char fpiece[5]; 	/* variable for position start-end of a piece */ 	char fname[SLEN]; /* variable for log file name */	int checkmate = 0; /* temporary variable for check mate */	/*	BOARD *Board = NULL;	Board = InitializeBoard();	*/	/* Display Board Function Here */			/* Menu Function Here */	Menu();	scanf("%d", &choose);	if (choose == 1)		{			printf("Choose Black or White to start the game: ");			do{				scanf("Please enter 1 for Black /or 2 for White: %d", &bw);						}while (bw != 1 && bw != 2);						printf("Game Start!!!\n");			if (bw == 2)		/* white go first */			{				do{					printf("Pick a Piece to move by enter its start and end position(Ex: a2a4): ");					scanf("%5s", fpiece);					/* Function call for make a move here*/					/* Check valid move Function here */					/* if invalid move made, then print form to ask for another move */					/* ADVANCE: Function for withdraw the move just made */					/* Function for checking if check mate here */									/* Function for machine make a move here */					/* *************************************** */								}while(checkmate == 0);			}						else		/*black wait for white go first then go */			{				do{					/* Function for machine make a move here */					/* *************************************** */									printf("Pick a Piece to move by enter its start and end position(Ex: a2a4): ");					scanf("%5s", fpiece);					/* Function call for make a move here*/					/* Check valid move Function here */					/* Function for checking if check mate here */					/* if invalid move made, then print form to ask for another move */					/* ADVANCE: Function for withdraw the move just made */									}while(checkmate == 0);						}		}			else if (choose == 2)		{			printf("Game Start!\n White go First!\n");			do{					printf("White Pick a Piece to move by enter its start and end position(Ex: a2a4): ");					scanf("%5s", fpiece);					/* Function for checking the White piece was chose instead of Black */										/* Function call for make a move here*/					/* Check valid move Function here */					/* if invalid move made, then print form to ask for another move */					/* ADVANCE: Function for withdraw the move just made */					/* Function for checking if check mate here */										printf("Black Pick a Piece to move by enter its start and end position(Ex: a2a4): ");					scanf("%5s", fpiece);					/* Function for checking the Black piece was chose instead of White */										/* Function call for make a move here*/					/* Check valid move Function here */					/* if invalid move made, then print form to ask for another move */					/* ADVANCE: Function for withdraw the move just made */					/* Function for checking if check mate here */								}while(checkmate == 0);		}			else if (choose == 3)		{			printf("Computer start playing game itself!! Enjoy the show!!\n");			do{				/* Function for machine make a move here */				/* *************************************** */			}while(checkmate == 0);					}							else if (choose == 4)		{			do{				printf("Please input the file name to read: "); /* optional cuz log file can be assigned to some unchanged name file */				scanf("%80s", fname);				y = ReadLog(fname);			}while(y ==1);									}	else if (choose == 5)		{			printf("SEE U NEXT TIME!!!\n");		}		/*	DeleteBoard(Board);		*/	return 0;}/* Menu Function */void Menu(){	printf("------------------------------\n");	printf("1: Start playing game with machine\n");		printf("2: ADVANCE: Start playing game with another human\n");	printf("3: ADVANCE: Computer start playing game with itself\n");		printf("4: Read to screen the Log file to see all moves was made \n");	printf("5: Exit\n");		printf("Please make your choice: ");		} /* end of Menu *//* Read log File Function */int ReadLog (char fname[SLEN]){	FILE           *File;	char            ftype[] = ".txt";	char            fname_tmp[SLEN];	int 			x ;	char c;		strcpy(fname_tmp, fname);	strcat(fname_tmp, ftype);	File = fopen(fname_tmp, "r");	if (!File) {		printf("Cannot open file \"%s\" for reading!\n", fname);		return 1;	}	else{	while((c = fgetc(File)) != EOF)	{		printf("%c", c); /* print single character in log file */		if ((x+1)%4 == 0 )			printf("\n");				x++;	}	printf("\n");	}		fclose(File);	return 0; }/* end of Read Log file *//* Display the Board */